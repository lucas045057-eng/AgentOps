# Database
DATABASE_URL=projects.db

# Comma-separated script IDs to execute when the service starts.
STARTUP_SCRIPT_IDS_RAW=
SCRIPT_TIMEOUT_SECONDS=300

# Optional SMTP failure notifications.
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_RECEIVER=

# Service
HOST=0.0.0.0
PORT=8000
DEBUG=false
